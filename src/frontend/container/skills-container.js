import React from 'react'

import ContainerWrapper from '../view/container-wrapper-view'

export default (props) => {
    return (
        <ContainerWrapper>
            
        </ContainerWrapper>
    )
}